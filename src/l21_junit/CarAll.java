/*
class Car {
    double fuelVolume = 0; // объём топлаива в литрах
    double maxFuelVolume; // объём бака
    double mileage = 0; // пробег в километрах
    double fuelConsumption; // расход топлива на 100км

    // создаем конструктор
    Car(double maxFuelVolume, double fuelConsumption) {
        this.maxFuelVolume = maxFuelVolume;
        this.fuelConsumption = fuelConsumption;
    }
}
    // создаем метод для заправки автомобиля
    void refuel(double liters) {
        if (fuelVolume + liters > maxFuelVolume) {
            fuelVolume = maxFuelVolume;
        } else {
            fuelVolume += liters;
        }
public class CarInfo {
    public static void main(String[] args) {

        Car polo = new Car(45,6);
//вызов метода
        polo.refuel(10); //вызов метода объекта
    }
}*/